# MySQL 数据库设计与 AI 编码规范

> 本文件用于约束 AI 在 Laravel 项目中进行 MySQL 表结构设计、Migration 编写、索引设计、数据迁移和数据库审查。默认面向 MySQL 8.0 与 Laravel Eloquent；若项目版本不同，必须先以项目实际版本为准。

## 1. 强制原则

1. 数据库负责可靠存储、数据完整性、唯一性和高效检索，不承载应用业务流程。
2. 业务规则和状态流转归 Service；数据查询与持久化归 Model/Eloquent。
3. 所有结构变更必须通过 Laravel Migration 管理，禁止只修改数据库而不提交 Migration。
4. 每张表、每个业务字段必须有准确的中文注释；注释说明业务含义，不重复字段名。
5. 表结构设计必须同时考虑：正确性、数据量、查询方式、并发、索引、扩展性和上线风险。
6. 禁止为“以后可能用到”随意添加大量备用字段。
7. 禁止使用 MySQL 关键字、拼音、无语义缩写或含义模糊的名称。
8. 不得擅自删除、改名或改变已有字段语义；破坏性变更必须明确迁移和回滚方案。

## 2. AI 开始设计前必须确认

AI 不得直接猜表结构。开始前必须阅读或确认：

- `composer.json` 中 PHP、Laravel 版本。
- MySQL 实际版本、字符集和排序规则。
- 项目已有 Migration、Model、Enum、关联关系和命名习惯。
- 业务实体、数据归属、生命周期和状态流转。
- 主要读写场景、列表筛选条件、排序字段和预计数据量。
- 是否多租户、是否需要软删除、审计、历史记录、幂等和附件。
- 时间、金额、单位、精度、时区和数据保留周期。

信息不足且会影响核心设计时，必须提出问题，不得静默假设。

## 3. 数据库与表的基础规范

### 存储引擎

- 业务表统一使用 `InnoDB`。
- 不得使用 MyISAM 等不支持事务或外键的引擎。

### 字符集与排序规则

- 默认使用 `utf8mb4`。
- 排序规则必须与项目和数据库现状保持一致，禁止同一业务库随意混用。
- 如果业务要求大小写敏感、二进制比较或特定语言排序，必须单独说明并选择合适 collation。

### 表名

- 遵循 Laravel 约定：小写 `snake_case`、复数形式，例如 `purchase_orders`。
- 中间表按项目约定命名；若无约定，使用两个单数模型名按字母顺序排列，例如 `role_user`。
- 表名必须表达稳定业务实体，不使用 `data`、`info`、`list`、`temp1` 等模糊后缀。
- 临时表、归档表、日志表必须在名称上明确用途。

### 表注释

- 每张业务表必须设置中文表注释。
- 注释示例：`客户订单主表`，而不是简单重复 `订单表`。

## 4. 字段命名规范

- 字段使用小写 `snake_case`。
- 主键统一为 `id`。
- 外键使用 `{实体单数}_id`，例如 `user_id`、`order_id`。
- 布尔值优先使用 `is_`、`has_`、`can_` 前缀，例如 `is_enabled`。
- 时间点使用 `_at`，例如 `paid_at`；日期使用 `_date`；纯时间使用 `_time`。
- 数量使用 `_count`，金额按存储方式使用 `_amount` 或 `_cents`，比率使用 `_rate`。
- 状态字段使用 `status` 或语义明确的 `{业务}_status`。
- 编号/单号使用 `_no`，第三方唯一标识可使用 `_id`，但必须避免与本地主键混淆。
- 禁止使用 `flag`、`type1`、`value`、`content2` 等无法体现含义的字段名。
- 同一含义在全项目中必须使用同一名称，禁止 `mobile`、`phone`、`tel` 混用。

## 5. 主键规范

- 默认使用 `BIGINT UNSIGNED AUTO_INCREMENT`，Laravel Migration 使用 `$table->id()`。
- 外键类型必须与目标主键完全一致，包括位数和 unsigned 属性。
- 不使用有业务含义的字段作为物理主键。
- 分布式系统确需 UUID/ULID 时，应统一全项目策略，并评估索引体积与写入性能。
- UUID 优先二进制紧凑存储或使用有序 UUID/ULID；不得在项目中混用多套 ID 方案。
- 对外暴露 ID 是否可枚举属于安全与产品决策，不得擅自改变主键策略。

## 6. 字段类型选择

### 字符串

- 短文本使用 `VARCHAR`，长度按真实业务上限设计，禁止习惯性全部使用 `VARCHAR(255)`。
- 固定长度且确实固定的编码可使用 `CHAR`，例如哈希或国家代码。
- 长文本使用 `TEXT`/`MEDIUMTEXT`，不得给长文本设置无意义默认值。
- 手机号、证件号、邮编、订单号等不可计算的数字必须使用字符串。
- IP 地址优先考虑 `VARBINARY(16)`；如项目更重视可读性，可使用 `VARCHAR(45)` 并保持一致。

### 整数

- 根据真实范围选择 `TINYINT`、`SMALLINT`、`INT`、`BIGINT`。
- 计数不得使用可能快速溢出的过小类型。
- 不允许出现负数的数量和标识可使用 unsigned，但必须考虑 Laravel 类型和计算兼容性。

### 布尔值

- Laravel 使用 `$table->boolean()`，MySQL 实际通常为 `TINYINT(1)`。
- 布尔字段必须明确默认值，并使用 `is_`/`has_`/`can_` 命名。
- 不得用 `0/1/2` 同时表达布尔和其他状态。

### 金额与精度数值

- 禁止使用 `FLOAT` 或 `DOUBLE` 存储金额。
- 金额优先使用最小货币单位整数，例如 `amount_cents BIGINT`；或统一使用 `DECIMAL(p,s)`。
- 采用 `DECIMAL` 时必须根据最大金额与小数位明确精度，例如 `DECIMAL(15,2)`，不得盲目套用。
- 多币种必须保存 `currency`，金额字段不得脱离币种解释。
- 比率、税率、经纬度、重量等必须根据业务精度选择 `DECIMAL`，并明确单位。
- 单位必须反映在字段名、注释或配套单位字段中，禁止仅靠开发人员猜测。

### 日期时间

- 业务时间点默认使用 `DATETIME`；是否使用 `TIMESTAMP` 必须基于范围、时区和自动转换需求决定。
- 全系统统一时间存储策略，通常数据库存 UTC、展示层转换用户时区。
- 使用 Laravel `$table->timestamps()` 创建 `created_at`、`updated_at`。
- 软删除使用 `$table->softDeletes()` 创建 `deleted_at`。
- 不使用 `0000-00-00 00:00:00` 表示未知时间，未知应使用 `NULL`。
- 仅存日期时使用 `DATE`，不要用字符串存日期。

### JSON

- JSON 只用于结构可变、非核心关联、非主要检索条件的数据。
- 核心业务字段、关联数据、需要约束或频繁筛选的数据必须结构化建列或建表。
- JSON 必须定义内部结构、版本和 Laravel cast。
- 需要按 JSON 属性高频查询时，应考虑生成列并建立索引。
- 禁止把整张表或多个业务实体塞入一个 JSON 字段逃避建模。

### Enum 与状态字段

- 默认使用可读的字符串状态或小整数 + PHP Enum，不强制使用 MySQL `ENUM`。
- 状态值必须有唯一含义和完整注释，不得用魔法数字散落在代码中。
- 状态新增频繁、跨系统共享或需要灵活扩展时，避免 MySQL `ENUM`。
- 简单且极稳定的闭合集合可在充分评估后使用 MySQL `ENUM`。
- 状态历史对审计重要时，应建立独立状态变更记录表，不能只保留当前状态。

## 7. NULL 与默认值

- `NULL` 表示未知、不适用或尚未发生；空字符串表示已知但内容为空，二者不得混用。
- 字段是否 nullable 必须由业务语义决定，不得为了写入方便全部 nullable。
- 必填字段使用 `NOT NULL`。
- 布尔、计数等字段通常应设置明确默认值，例如 `0`。
- 业务状态默认值必须代表真实初始状态。
- 不给时间、金额、外键设置会掩盖数据错误的虚假默认值。
- 禁止使用 `0` 作为不存在的外键，未关联应使用 `NULL`。
- 默认值不得依赖易变化的业务规则。

## 8. 通用字段设计

业务主表通常根据实际需要包含：

```text
id            主键
tenant_id     租户标识（多租户项目）
status        当前业务状态
created_by    创建人（需要审计时）
updated_by    最后更新人（需要审计时）
created_at    创建时间
updated_at    更新时间
deleted_at    软删除时间（需要软删除时）
```

规则：

- 不得机械给所有表增加 `created_by`、`updated_by`、`deleted_at`，必须符合业务需求。
- 关联表、不可变日志表可以没有 `updated_at`，但必须明确原因。
- 审计字段保存的是操作主体，不等同于业务归属人。
- 需要记录来源时使用明确字段，如 `source`、`source_id`，并定义合法值。

## 9. 关系设计

### 一对多

- 外键放在“多”的一方。
- 外键 nullable 与删除策略必须符合业务语义。

### 多对多

- 使用独立关联表，并为两个外键建立联合唯一索引，防止重复关联。
- 如果关联本身有状态、有效期、创建人等业务属性，应将其视为正式业务实体。

### 一对一

- 外键必须有唯一索引保证数据库层一对一。
- 只有字段生命周期、权限、体积或访问频率明显不同才拆表，禁止无意义纵向拆分。

### 多态关联

- 谨慎使用 Laravel polymorphic relation。
- 多态字段缺乏数据库外键约束，必须评估数据完整性、查询性能和类型重命名风险。
- 使用 morph map，禁止直接长期存储 PHP 完整类名。
- 核心财务、权限等强一致关系优先显式建表和外键。

## 10. 外键约束

- 默认应评估并优先建立数据库外键，除非项目架构明确不使用。
- 外键列必须建立索引；MySQL 通常会为约束创建所需索引，但设计中仍应明确。
- 删除策略必须显式选择：
  - `RESTRICT/NO ACTION`：关键业务数据默认选择，防止误删。
  - `CASCADE`：仅适用于生命周期完全从属于父记录的数据。
  - `SET NULL`：关系可以解除且字段允许 NULL 时使用。
- 禁止对订单、账单、支付、审计等关键数据随意级联删除。
- 软删除不会触发数据库级联软删除；相关策略必须在业务层明确设计。
- 引入外键前必须确认历史数据干净且字段类型完全一致。

## 11. 索引设计

### 必须建立索引的常见字段

- 主键和唯一业务编号。
- 外键、租户字段。
- 高频筛选、关联、排序和分组字段。
- 幂等键、第三方唯一流水号。

### 联合索引

- 联合索引顺序必须依据真实 SQL 的筛选、范围和排序方式设计。
- 遵守最左前缀原则；通常等值条件在前，范围/排序字段在后，但必须结合数据选择性与查询验证。
- 多租户查询通常以 `tenant_id` 作为联合索引前缀，例如 `(tenant_id, status, created_at)`。
- 唯一约束在多租户场景通常应包含 `tenant_id`。
- 禁止给所有筛选字段单独建索引，联合查询不一定能有效利用多个单列索引。

### 索引约束

- 索引名称应清晰，可遵循 Laravel 自动命名或项目统一命名。
- 控制索引数量；每个索引都会增加存储和写入成本。
- 低区分度字段（如布尔值）通常不单独建索引，应与其他条件组合评估。
- `TEXT/BLOB` 不得直接建立超长普通索引；应使用合理前缀、全文索引或重构查询。
- 对索引列使用函数、隐式类型转换或前置通配符可能导致索引失效。
- 索引设计必须用真实或近似 SQL 通过 `EXPLAIN` 验证，不能只凭字段名称猜测。

## 12. 唯一约束与软删除

- 业务唯一性必须用数据库唯一索引兜底，不能只靠“先查询再写入”。
- 多租户唯一性示例：`UNIQUE (tenant_id, code)`。
- MySQL 唯一索引允许多个 `NULL`，设计时必须明确是否符合业务。
- `UNIQUE(email, deleted_at)` 不能可靠实现“未删除数据唯一”，因为 `deleted_at = NULL` 可重复。
- 软删除后允许复用唯一值时，必须选择明确方案，例如：
  - 删除时改写唯一字段并保留原值到审计字段；
  - 使用生成列映射“有效记录”并建立唯一索引；
  - 不允许复用，保持全生命周期唯一。
- AI 必须说明采用哪种方案及其恢复数据时的冲突策略。

## 13. 多租户设计

- 共享数据库/共享表模式必须在所有租户业务表保存 `tenant_id`。
- Model 查询、唯一索引、联合索引和外键关系都必须考虑租户边界。
- 禁止只依赖前端传入 `tenant_id`；租户身份应来自认证上下文。
- 关联数据必须防止跨租户引用。
- 高频查询索引通常以 `tenant_id` 开头，但必须通过实际查询验证。
- 全局数据与租户数据应明确区分，禁止用 `tenant_id = 0` 模糊表达全局数据。
- 租户删除、停用、数据导出和数据保留必须有明确策略。

## 14. 软删除、归档与物理删除

- 需要恢复、审计或避免误删的业务主数据可使用软删除。
- 日志流水、关联表和高容量临时数据不应默认全部软删除。
- 软删除表的所有查询必须明确是否包含已删除数据。
- 软删除不能替代审计日志；它只记录删除时间，不记录操作原因和完整变化。
- 大表必须制定归档、分区或物理清理策略，不能无限增长。
- 物理删除关键数据前必须明确依赖关系、合规要求和可恢复方案。

## 15. 文件与附件数据

- 文件二进制默认存对象存储或文件系统，数据库保存元数据，不直接存大型 BLOB。
- 附件表至少根据需要包含：业务归属、原始文件名、存储磁盘、存储路径、MIME、扩展名、大小、哈希、上传人和时间。
- 存储路径不得等同于公开 URL；访问 URL 应由存储服务动态生成。
- 不保存永久签名 URL。
- 一份文件关联多个实体时，应区分“文件实体”和“业务附件关系”。
- 文件删除必须考虑数据库记录、实际对象和延迟清理的一致性。

## 16. 审计、日志与历史记录

- 关键业务状态变更应建立独立历史表，至少记录业务实体、前状态、后状态、操作人、时间、原因和上下文。
- 审计日志应尽量追加写，禁止随意更新或删除。
- 操作日志与普通应用日志用途不同，不得混为一张万能日志表。
- 快照字段用于保留当时事实，例如订单收货地址，不应只依赖可能变化的用户当前资料。
- 历史表必须考虑数据量、时间索引和归档策略。
- 敏感信息不得无必要复制到日志或快照中。

## 17. 并发、事务与幂等

- 数据库唯一索引是防重复写入的最终保障之一。
- 库存、余额、次数等并发更新优先使用带条件的原子更新，不使用“读取后直接覆盖”。
- 必须检查受影响行数，判断条件更新是否成功。
- 需要锁定数据时可使用 `SELECT ... FOR UPDATE` 对应的 Eloquent 锁，但事务范围必须小。
- 事务由 Service/Action 控制，Migration 不承担运行时业务事务。
- 外部请求不可逆操作应保存幂等键、请求号、第三方流水号和处理状态。
- 幂等键必须具有唯一约束，并明确作用域与有效期。
- 必须评估死锁；发生死锁时可有限重试，但不能用无限重试掩盖设计问题。

## 18. 分表、分区与冗余字段

- 未达到明确数据量和性能瓶颈前，不提前分库分表。
- 分区必须基于真实访问和清理模式，且确认 MySQL 版本、唯一键和外键限制。
- 冗余字段只在性能或历史快照有明确收益时使用。
- 冗余字段必须指定唯一事实来源、更新时机和一致性修复方案。
- 统计值、余额、计数缓存必须能够校验或重建。
- 不得用过度反范式化逃避正确关系设计。

## 19. Laravel Migration 规范

- 一个 Migration 聚焦一个明确结构变更。
- 文件名、类和方法应表达变更目的。
- 新表使用 `Schema::create()`，修改表使用 `Schema::table()`。
- 表、字段必须写中文 `comment()`。
- 外键、唯一约束、普通索引必须在 Migration 中显式体现。
- `down()` 必须与 `up()` 对称并可执行；不可逆数据操作必须明确标注，不得伪造回滚。
- 禁止在 Migration 中调用 Eloquent Model，避免未来 Model 变化导致旧 Migration 失效。
- 数据回填优先使用独立、可控的迁移脚本或 Query Builder，并考虑批处理。
- 大表新增非空列、修改类型、创建索引前必须评估锁表和在线 DDL 能力。
- 生产 Migration 不得假设数据库为空或数据完全合法。
- 重命名字段前必须检查应用兼容窗口，必要时采用“新增—双写—回填—切换—删除”步骤。

### 推荐示例

```php
Schema::create('orders', function (Blueprint $table) {
    $table->id()->comment('订单主键');
    $table->unsignedBigInteger('tenant_id')->comment('所属租户主键');
    $table->string('order_no', 32)->comment('租户内唯一订单编号');
    $table->unsignedBigInteger('user_id')->comment('下单用户主键');
    $table->char('currency', 3)->default('CNY')->comment('ISO 4217 币种代码');
    $table->unsignedBigInteger('total_amount_cents')->comment('订单总金额，单位：分');
    $table->string('status', 32)->default('pending')->comment('订单状态');
    $table->dateTime('paid_at')->nullable()->comment('支付完成时间');
    $table->timestamps();
    $table->softDeletes();

    $table->unique(['tenant_id', 'order_no'], 'uk_orders_tenant_order_no');
    $table->index(['tenant_id', 'status', 'created_at'], 'idx_orders_tenant_status_created');
    $table->index('user_id', 'idx_orders_user_id');

    $table->foreign('tenant_id')->references('id')->on('tenants')->restrictOnDelete();
    $table->foreign('user_id')->references('id')->on('users')->restrictOnDelete();
});

DB::statement("ALTER TABLE orders COMMENT = '客户订单主表'");
```

> 示例只表达规范，实际字段、索引、删除策略和金额精度必须依据真实业务调整。

## 20. 查询与性能规则

- API 列表必须分页并设置最大每页数量，禁止无界查询。
- 大数据处理使用 `chunkById()`、`lazyById()` 或游标，避免一次性加载。
- 禁止在循环内执行查询，主动避免 N+1。
- 查询只选择需要的字段，但使用 Eloquent 关联时必须保留关联键。
- 高频查询应记录实际 SQL，并使用 `EXPLAIN`/`EXPLAIN ANALYZE` 验证。
- 慢查询优化应先确认执行计划、扫描行数、回表、排序、临时表和锁等待。
- 禁止仅通过添加索引解决所有性能问题；也要检查查询结构、数据模型和访问方式。
- `LIKE '%keyword%'` 普通 B-Tree 索引通常无法有效利用，应评估全文检索或搜索服务。
- 深分页不得长期依赖巨大 OFFSET，优先评估基于游标/主键的分页。

## 21. 安全与敏感数据

- 密码只能存安全哈希，禁止存明文或可逆加密密码。
- Token、密钥、证件号、手机号等敏感字段必须按业务和合规要求进行哈希、加密或脱敏。
- 需要精确查找的加密字段可设计独立盲索引/哈希字段，但必须评估碰撞和密钥管理。
- 禁止在表注释、默认值、Migration 或 Seeder 中写入真实密钥和生产账号。
- 测试数据不得使用真实个人敏感信息。
- 数据库账号遵循最小权限，应用运行账号不得拥有无必要的 DDL 或管理权限。

## 22. Seeder 与测试数据

- Seeder 用于基础数据或开发/测试数据初始化，不用于替代生产数据迁移流程。
- Seeder 必须可重复运行或明确说明非幂等性。
- Factory 数据应符合字段约束和真实业务关系。
- 测试必须覆盖唯一约束、外键、nullable、默认值、软删除和关键并发场景。
- 不在生产 Seeder 中写死环境相关 ID。

## 23. 禁止事项

AI 设计数据库时禁止：

- 所有字段统一 `VARCHAR(255)`。
- 使用 `FLOAT/DOUBLE` 存金额。
- 用字符串保存本应可计算、排序的日期或数值。
- 只做应用层唯一校验而不建唯一索引。
- 省略主键、必要索引、字段注释或表注释。
- 通过逗号分隔字符串保存多个 ID。
- 用 JSON 代替核心关系表。
- 使用 `0` 代替空外键。
- 对关键业务数据随意 `CASCADE DELETE`。
- 在生产大表上直接执行未经评估的阻塞 DDL。
- 在 Migration 中调用随版本变化的 Model 或 Service。
- 建立大量重复、覆盖关系不清的索引。
- 仅凭字段名设计索引而不结合 SQL。
- 把当前表结构当作审计历史。
- 无明确方案地同时使用软删除和唯一字段。
- 为追求“灵活”建立万能 EAV 表。

## 24. AI 输出数据库设计时的固定格式

AI 提交表设计时必须包含：

1. **业务说明**：该表解决什么问题，数据生命周期是什么。
2. **表关系**：一对一、一对多、多对多及删除策略。
3. **字段清单**：字段名、类型、长度/精度、是否为空、默认值、索引、中文注释。
4. **索引说明**：每个索引服务于哪些查询，联合索引顺序的原因。
5. **完整性约束**：主键、唯一约束、外键、状态和幂等约束。
6. **Laravel 实现**：Migration、Model casts/relations，以及必要的 PHP Enum。
7. **风险说明**：数据量、并发、软删除唯一性、迁移锁表、兼容和回滚风险。
8. **验证结果**：Migration、测试、`EXPLAIN` 或静态检查的实际执行结果。

## 25. AI 强制自检清单

完成数据库设计或 Migration 后，AI 必须逐项确认：

- [ ] 已确认 MySQL、Laravel 和 PHP 的实际版本。
- [ ] 表名、字段名符合 Laravel 与项目现有规范。
- [ ] 表及所有业务字段均有准确中文注释。
- [ ] 主键和外键类型完全一致。
- [ ] 必填、nullable 和默认值符合真实业务语义。
- [ ] 金额未使用浮点类型，并明确币种与单位。
- [ ] 日期时间类型和时区策略明确。
- [ ] 状态值有统一 Enum/常量和合法初始值。
- [ ] 业务唯一性由数据库唯一约束保证。
- [ ] 已检查软删除与唯一索引冲突。
- [ ] 多租户字段、查询、索引和唯一约束包含租户边界。
- [ ] 外键删除策略不会误删关键数据。
- [ ] 索引对应真实筛选、关联、排序或唯一需求。
- [ ] 不存在明显重复索引、超宽索引或无效低选择性单列索引。
- [ ] 已考虑 N+1、无界查询、深分页和大数据批处理。
- [ ] 并发更新、库存/余额操作和幂等写入有数据库保障。
- [ ] JSON、冗余字段和多态关系均有充分理由。
- [ ] Migration 不调用 Model/Service，`down()` 与 `up()` 对应。
- [ ] 大表 DDL、数据回填、兼容窗口和回滚风险已评估。
- [ ] 敏感数据已考虑加密、哈希、脱敏和最小权限。
- [ ] 已补充约束、关联和关键查询相关测试。
- [ ] 已如实报告尚未验证或仍有风险的部分。

## 26. AI 判断口诀

- 能否为空：看业务语义，不看编码是否方便。
- 用什么类型：看数据本质、范围和精度，不看前端展示形式。
- 是否建索引：看真实 SQL 和数据量，不看字段是否“重要”。
- 是否唯一：看业务不变量，并由数据库约束兜底。
- 是否拆表：看实体、生命周期、权限和访问模式，不看字段数量。
- 是否用 JSON：看结构是否真正可变、是否需要约束和查询。
- 是否软删除：看恢复、审计和保留需求，不是所有表默认开启。
- 是否使用外键：看一致性、架构和运维约束，并明确删除策略。
- 是否冗余：看性能收益与一致性方案，不能只图查询方便。
- 是否分库分表：看已验证的容量瓶颈，不做无证据的提前复杂化。
